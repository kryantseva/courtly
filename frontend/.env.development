# Локально: `npm run dev` в каталоге frontend — Vite подхватит этот файл.
# Пустой VITE_API_URL → запросы на тот же origin, прокси в vite.config.js шлёт /api на backend :8000
VITE_USE_API=true
VITE_API_URL=
